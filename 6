#!/bin/bash

# Name of the running container
CONTAINER_NAME="gl-exporter-container"

# Execute commands inside the running container
docker exec $CONTAINER_NAME /bin/bash -c "./env && gl_exporter -f export.csv -o migration-archive.tar.gz"

# Exit the script; container remains running
